import express from 'express';
const router = express.Router();

//para mostrar los foros en la página de inicio
router.get('/inicio', (req, res) => {
  // Lógica para obtener y mostrar los foros
});

export default router;
